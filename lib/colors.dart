// colors.dart (optional helper file)

import 'package:flutter/material.dart';

class OraColors {
  // Marian blue (primary accent)
  static const primary = Color(0xFF335C99);   // Marian blue

  // Supporting deep blue (for gradients or headers if needed)
  static const primaryDeep = Color(0xFF223A6E);

  // Gold accent (matches your logo background mood)
  static const gold = Color(0xFFC8A14F);

  // Neutrals
  static const background = Color(0xFFF5F1E8); // warm off-white
  static const surface = Colors.white;         // base for cards
  static const text = Color(0xFF1F261E);
  static const muted = Color(0xFF667063);

  // Success / “done” state
  static const success = Color(0xFF4F7A45);
}